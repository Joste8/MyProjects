{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow border-0">
                <div class="card-body p-4">
                    <h3 class="mb-4">Edit Subcategory</h3>
                    {{ form_start(form) }}
                        {{ form_widget(form, {'attr': {'class': 'form-control mb-3'}}) }}
                        <button class="btn btn-success w-100 mt-3">Update Subcategory</button>
                    {{ form_end(form) }}
                    <hr>
                    <a href="{{ path('app_subcategory_index') }}" class="d-block text-center text-muted mt-2">Back to List</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}