{% extends 'base.html.twig' %}

{% block title %}ProductAttributeValue{% endblock %}

{% block body %}
    <h1>ProductAttributeValue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_attribute_value.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_attribute_value_index') }}">back to list</a>

    <a href="{{ path('app_product_attribute_value_edit', {'id': product_attribute_value.id}) }}">edit</a>

    {{ include('product_attribute_value/_delete_form.html.twig') }}
{% endblock %}
