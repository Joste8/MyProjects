{# Variants Column - ഇവിടെയാണ് വേരിയന്റ് വാല്യൂസ് വരുന്നത് #}
<td>
    {% if product.variants is not empty %}
        <div class="d-flex flex-wrap gap-1">
            {% for variant in product.variants %}
                <span class="badge border border-secondary text-light">
                    {{ variant.name }} ({{ variant.stock }})
                </span>
            {% endfor %}
        </div>
    {% else %}
        <span class="text-muted small">None</span>
    {% endif %}
</td>

{# Action Column - പുതിയ വാല്യൂ ആഡ് ചെയ്യാനുള്ള ബട്ടൺ #}
<td>
    <a href="{{ path('admin_product_variant_new', {'product_id': product.id}) }}" class="btn btn-sm btn-warning text-dark fw-bold">
        + Variant Value
    </a>
</td>