<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Arial', sans-serif; font-size: 12px; color: #333; }
        .receipt-box { width: 100%; border: 1px solid #ccc; padding: 10px; }
        .header { text-align: center; border-bottom: 2px solid #000; padding-bottom: 5px; }
        .footer { text-align: center; margin-top: 20px; border-top: 1px dashed #000; padding-top: 5px; }
    </style>
</head>
<body>
    <div class="receipt-box">
        <div class="header">
            <h3>MY STORE</h3>
            <p>Purchase Receipt</p>
        </div>
        <p><strong>Date:</strong> {{ log.createdAt|date('d-m-Y H:i') }}</p>
        <p><strong>Customer:</strong> {{ log.customerName }}</p>
        <hr>
        <p><strong>Product:</strong> {{ log.product.name }} ({{ log.variant.value }})</p>
        <p><strong>Qty:</strong> {{ log.quantity }}</p>
        <div class="footer">
            <p>Thank you for shopping!</p>
        </div>
    </div>
</body>
</html>